# T07 handoff — 2026-09-08

## 1. Goal

Continue the fixed five-day production observation without manufacturing dates,
while keeping the prepared Settings refactor isolated from the live study.

## 2. Current state

Observation day two is complete in production. The fixed plan remains
`8848a88f-3814-43dc-a454-f87007b61ba7`, covering 2026-09-07 through 2026-09-11.

| Day | Seoul date | Planned | Actual | Records | Ratio |
| --- | --- | ---: | ---: | ---: | ---: |
| 1 | 2026-09-07 | 30 min | 30 min | 1 | 1.00x |
| 2 | 2026-09-08 | 30 min | 8 min | 1 | 0.27x |

The day-two execution is the real 02:18–02:26 KST verification session. The task
was completed after the checks passed.

The one required rule change was saved at 02:27 KST, after day two and before
day three. It cites exactly the day-one and day-two records. Before the change,
the production summary is 38 actual / 60 planned = 0.63x.

- Before: use the default 30-minute estimate for each task.
- After: before starting, split work into 10-minute units and estimate every item
  separately.
- Reason: day one was 1.00x but day two was 0.27x, so the default estimate left a
  large gap.

Production says day-three recording is now unblocked. No deployment occurred.

## 3. Next action — 2026-09-09 KST only

1. Open `https://t06-plando-see-diary.onrender.com/app` and select
   `T07 5일 실제 관찰`.
2. Following the new rule, create the real day's work as one or more 10-minute
   units and give each item its own estimate before starting.
3. Perform the work, then save the actual start/end and actual minutes. Do not
   backdate or round the interval to make the ratio look better.
4. Confirm the day-three row has one or more records and appears after the saved
   rule-change timestamp.
5. Record the result in `docs/STATUS.md`; do not edit the frozen study protocol.

Do not add any day-three execution before 2026-09-09 Seoul time.

## 4. Settings branch

Working branch: `feature/settings-page`, tracking
`origin/feature/settings-page`. The daily `/app` ends after Plan·Do·See and the
protected `/settings` route contains profile, export, password change, and
account deletion.

Desktop visual QA on 2026-09-08 covered dark and light themes, the top navigation,
profile, export, security controls, and the deletion warning. No visible defect
was found. Mobile browser QA is still required after the five-day capture.

Do not merge or deploy this branch until the 2026-09-11 evidence is safely
captured.

## 5. Verification completed today

```text
frontend: 76 passed
frontend production build: passed
backend: 318 passed, 4 skipped
secret audit: 0 findings across worktree, build, and 1,456 Git objects
git diff --check: passed before documentation changes
```

The skips are expected: one needs the completed real five-day evidence file and
three require `TEST_DATABASE_URL` for PostgreSQL-only checks.

## 6. Commands for the next code session

```powershell
git fetch origin
git switch feature/settings-page
git pull --ff-only
backend/.venv/Scripts/python.exe -m pytest backend/tests -o addopts='' -q -rs
npm --prefix frontend test
npm --prefix frontend run build
backend/.venv/Scripts/python.exe backend/scripts/audit_secrets.py
git diff --check
```

## 7. Do not change

- Do not edit `docs/T07-STUDY-PROTOCOL.md` during 2026-09-07~11.
- Do not add, edit, delete, or backdate real observation records to improve a
  result.
- Do not create a second rule change.
- Do not change the observation plan ID or re-arm `BOOT_TASK`.
- Do not deploy during the observation window.
- Do not commit production diary contents, account details, credentials, local
  databases, exports, or screenshots containing them.
- Preserve the pre-claim Neon backup branch.

## 8. Git state

This handoff is written on `feature/settings-page`. Inspect the final handoff
commit with `git rev-parse HEAD`; the file does not pin its own hash. No intended
uncommitted file should remain after the handoff commit.
