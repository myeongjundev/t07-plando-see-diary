# T07 handoff — 2026-09-05

## Latest update — PostgreSQL gate passed

The access blocker in section 5 below is historical: GitHub sign-in now exposes
the correct Neon/Render resources. Full PostgreSQL suite: 312 passed, 1 skipped.
Test branch: `br-snowy-salad-a65iof8l` (schema-only, expires Sep 6 at 19:57 KST).
Backup `br-rough-feather-a6lo5utr` contains 7 plans, 8 tasks, 3 logs, 1 reflection.
Live API partition matches render.yaml; all four excluded plans have no active
tasks and the two excluded task IDs are already soft-deleted in the backup.

Render env has been saved with Save only, including independent 32-byte keys and
BOOT_TASK/claim lists. Existing DATABASE_URL and claim credentials preserved.
Source modal has the T07 public URL prepared; not connected/deployed yet.
Execution-time cleanup confirmation is pending. After approval, repoint and
manually deploy, verify the claim, then set BOOT_TASK=none. A public repository
source does not automatically deploy pushes without a Blueprint.

## 1. Goal

Complete deployment preparation, then deploy T07 on the existing T06 Render/Neon
service and begin the actual-use period. User authorized proceeding from status.

## 2. Current state

Six-section guide and T07 submission draft now exist. User judgment is explicitly
pending. Runtime authentication code is unchanged. Corrected and regenerated the
synthetic evidence; original-cookie logout replay now reaches 401 and both valid
JSON cross-account deletion attempts reach 404. Cookie reporting respects Path.
Real user IDs are used in query/header/body forgery and both plan/task counts stay
unchanged. Production is unchanged; no claim or deletion was performed.

## 3. Run commands

From the T07 repository in PowerShell:

```powershell
backend/.venv/Scripts/python.exe backend/scripts/collect_auth_evidence.py
backend/.venv/Scripts/python.exe -m pytest backend/tests -o addopts='' -q -rs
backend/.venv/Scripts/python.exe backend/scripts/audit_secrets.py
git diff --check
```

## 4. Passed checks

Full backend 309 passed / 4 skipped. After final collector changes, regeneration
succeeded and guide/evidence/matrix checks passed (15 passed / 1 skipped).
Secret-pattern scan: 0 findings over worktree, frontend build and 1,308 Git objects.
Guide version/sections/T06 ancestry checks exist; promised test coverage is 46/47.
This proves document structure and local behavior, not completion of C40/C07 or
production verification. Earlier same-session frontend: 71 tests and build passed.

## 5. Unrun and access blockers

Neon browser login reached an account with one organization and no projects.
Render browser remained at sign-in. Asked user to log in to the accounts containing
the existing T06 resources. No database environment variables or local .env file
were available. Docker CLI exists but its engine is stopped.

Three PostgreSQL tests remain unrun. With the five new tests the full-suite target
after PostgreSQL setup is 312 passed / 1 skipped. The final coverage skip concerns
the still-unwritten real five-day check, not a failed authentication test.

## 6. Next action

Once correct dashboard access is available, follow `T07-HANDOFF-2026-09-04.md`
section 4: empty schema-only Neon test branch → PostgreSQL tests → production
backup → Render environment/repository configuration → claim and migrations →
verify ownership and disable BOOT_TASK → create observation plan and record ID.
The claim includes the previously classified excluded rows; review actual target
counts and backup before applying. Record deployed library versions and perform
the outstanding Render hash benchmark. Start the real five-day period the next day.

## 7. Do not change

Keep official criteria and the observation protocol fixed. Do not manufacture
personal judgments, actual-use timestamps or production evidence. Do not put
credentials or private diary text in documents. Google OAuth2 remains a target.

## 8. Changed files

- `backend/scripts/collect_auth_evidence.py`: correct recorded security scenarios.
- `backend/tests/acceptance/test_t07_evidence.py`: regression coverage for evidence.
- `backend/tests/acceptance/test_t07_guide.py`: promised document and ancestry checks.
- `docs/T07-EVIDENCE/01` through `11`: regenerated, synthetic and masked.
- `docs/T07-AUTH-GUIDE.md`, `docs/T07-SUBMISSION.md`: reviewable submission drafts.
- `docs/T07-ACCEPTANCE-MATRIX.md`: T07 submission pointers and coverage note.
- `docs/STATUS.md`, this handoff: current state and next action.

## 9. Git state

Start HEAD: `b6b190cd9a73cdb51fed154a31ad7acdc319908e`, clean main.
Changes above are local and uncommitted. No push or production deployment performed.
