# T07 handoff — 2026-09-07

## 1. Goal

Prepare the approved settings/profile information architecture without changing
the live five-day observation, and leave the next observation step explicit.

## 2. Current state

The local `feature/settings-page` branch has a protected `/settings` route. The
daily `/app` page now ends after Plan·Do·See; its top account menu opens Settings.
Settings shows profile information, JSON export, password change and the existing
password-confirmed account deletion flow. It links back to the diary and retains
logout and theme controls.

Production was deliberately not deployed. The fixed observation plan continues on
the existing live build. Day one has a user-entered 30-minute result; timestamp
evidence still needs to be captured before final submission.

## 3. Run commands

From `C:\gov\project\skt aleph\t07-plando-see-diary` in PowerShell:

```powershell
backend/.venv/Scripts/python.exe -m pytest backend/tests -o addopts='' -q -rs
npm --prefix frontend test
npm --prefix frontend run build
backend/.venv/Scripts/python.exe backend/scripts/audit_secrets.py
git diff --check
```

## 4. Passed acceptance IDs

- Frontend: 76 tests passed, including anonymous and signed-in `/settings`
  routing, Settings contents, navigation in both directions, password-change
  safeguards and account-deletion safeguards.
- Backend: 318 passed, 4 skipped. `SPA_ROUTES` now verifies `/settings` shell and
  `no-store` behavior while unknown subpaths remain 404.
- Production frontend build passed.
- T07-C97, C109, C114 and C134 behavior is preserved by the refactor.

## 5. Failed or unrun acceptance IDs

No code failure is known. Visual browser QA for the local settings page is unrun;
the desktop browser-control runtime could not initialize in this session. The real
five-day observation is intentionally incomplete, and three PostgreSQL-only checks
remain skipped without `TEST_DATABASE_URL`.

## 6. Next action

On 2026-09-08, record one real day-two execution in the fixed observation plan.
After that record exists, make the one protocol rule change before recording day
three. Keep this settings branch local until all five dates and their evidence are
captured; then visually verify it, merge to `main`, deploy once, and recheck
`/settings`, `/app`, export and the deletion warning.

## 7. Do not change

Do not deploy during the five-day observation, edit
`docs/T07-STUDY-PROTOCOL.md`, manufacture dates, alter the observation plan ID,
re-arm `BOOT_TASK`, expose credentials, or use production diary text in committed
evidence. Preserve the pre-claim Neon backup branch.

## 8. Changed files

- `frontend/src/App.tsx`: removes administrative panels from the daily flow.
- `frontend/src/main.tsx`, `backend/app/__init__.py`: add protected/direct
  `/settings` routing.
- `frontend/src/auth/AccountBar.tsx`: adds Settings/Diary navigation while
  retaining logout.
- `frontend/src/features/account/SettingsPage.tsx`: profile and settings page.
- `frontend/src/features/account/AccountPanel.tsx`,
  `frontend/src/features/export/ExportPanel.tsx`: reuse existing protected actions
  in the new section order.
- `frontend/src/styles.css`: desktop/mobile settings and account navigation styles.
- route/settings tests: verify the new navigation and access gate.
- `README.md`, `docs/T07-ARCHITECTURE.md`, `docs/DECISIONS.md`, `docs/STATUS.md`:
  document the current behavior and D-055.

## 9. Git state

Start commit: `0330655` on clean `main`.

Working branch: `feature/settings-page`. The implementation is intentionally not
pushed to `main` and not deployed during observation. End commit: this handoff is
part of that commit, so inspect it with `git rev-parse HEAD` instead of writing a
self-invalidating hash here. No intended uncommitted path remains after handoff.
