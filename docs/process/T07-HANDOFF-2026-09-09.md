# T07 handoff — 2026-09-09

## 1. Goal

Continue the fixed five-day production observation with real Seoul-date work,
while keeping the prepared Settings refactor isolated from the live study.

## 2. Observation through day three

The fixed `T07 5일 실제 관찰` plan covers 2026-09-07 through 2026-09-11.

| Day | Seoul date | Planned | Actual | Records | Ratio |
| --- | --- | ---: | ---: | ---: | ---: |
| 1 | 2026-09-07 | 30 min | 30 min | 1 | 1.00x |
| 2 | 2026-09-08 | 30 min | 8 min | 1 | 0.27x |
| 3 | 2026-09-09 | 10 min | 4 min | 1 | 0.40x |

Day three followed the changed rule in the required order. A 10-minute task for
the day's status check and record cleanup was created before work began. The
real execution interval was 00:05–00:09 KST, the execution was saved afterward,
and the task was then marked complete. Production showed one day-three record
and the `0.40배` ratio.

The one rule change remains the entry saved on day two before today's execution:

- Before: use the default 30-minute estimate for each task.
- After: before starting, split work into 10-minute units and estimate every item
  separately.
- Basis: days one and two differed at 1.00x and 0.27x; their pre-change total is
  38 actual / 60 planned = 0.63x.

The post-change total is currently 4 actual / 10 planned = 0.40x. No second rule
change and no deployment occurred.

## 3. Next action — 2026-09-10 KST only

1. Open production and select `T07 5일 실제 관찰`.
2. Before starting real work, create one or more 10-minute task units with a
   separate estimate for each.
3. Perform the work, then save its true start, end, and actual minutes. Do not
   backdate or pad the interval.
4. Mark the task complete and confirm the day-four row has a real record.
5. Update `docs/STATUS.md` and add the 2026-09-10 handoff without editing the
   frozen protocol.

Do not add a day-four execution before 2026-09-10 Seoul time.

## 4. Verification completed today

```text
frontend: 76 passed
frontend production build: passed
backend: 318 passed, 4 skipped
secret audit: 0 findings across worktree, build, and 1,481 Git objects
git diff --check: passed before documentation changes
```

The skips are expected: one requires the completed five-day evidence file and
three require `TEST_DATABASE_URL` for PostgreSQL-only checks.

## 5. Branch and continuation

Work remains on `feature/settings-page`, tracking
`origin/feature/settings-page`. Pull it with `git pull --ff-only` before
continuing at the academy. The protected `/settings` refactor remains local to
this branch and must not be merged or deployed until all five observation days
are safely captured.

Useful verification commands:

```powershell
npm --prefix frontend test
npm --prefix frontend run build
backend/.venv/Scripts/python.exe -m pytest backend/tests -o addopts='' -q -rs
backend/.venv/Scripts/python.exe backend/scripts/audit_secrets.py
git diff --check
```

## 6. Do not change

- Do not edit `docs/T07-STUDY-PROTOCOL.md` during 2026-09-07 through 2026-09-11.
- Do not add, edit, delete, or backdate real observation records to improve the
  result.
- Do not create a second rule change.
- Do not change the observation plan ID or re-arm `BOOT_TASK`.
- Do not deploy during the observation window.
- Do not commit production diary contents, account details, credentials, local
  databases, exports, or screenshots containing them.
- Preserve the pre-claim Neon backup branch.

After committing this handoff, the working tree should be clean and the branch
should match `origin/feature/settings-page`.
