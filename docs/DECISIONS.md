# T06 decision log

Record material decisions here. Do not rewrite old decisions; add a superseding row.

| ID | Status | Decision | Reason | Affected requirements |
|---|---|---|---|---|
| D-001 | Active | Keep the project in preparation mode until the official card is reconciled. | Preliminary material conflicts with the course overview. | All |
| D-002 | Active | Use only synthetic or masked data in public and submitted artifacts. | The preliminary assignment explicitly keeps real records on the PC. | Privacy, five-day use |
| D-003 | Candidate | Prefer React + Flask + SQLAlchemy + PostgreSQL if server DB remains required. | Keeps the existing frontend workflow while adding an independently testable Python API. | Storage, deployment |
| D-004 | Candidate | Use study-session minutes as the diary subject. | Easy daily use and a clear single unit, but this remains the user's decision. | Fields, summary |
| D-005 | Active | Supersede D-001: begin implementation after reconciling the official source received on 2026-09-01. | The final five cards and 44 acceptance IDs are now available. | All |
| D-006 | Active | Supersede D-003: use React + Vite, Flask + SQLAlchemy, and PostgreSQL; tests may substitute SQLite in memory. | The assignment requires a real server database, and the user chose Flask for this project. | T06-C04–T06-C36, T06-C78–T06-C83 |
| D-007 | Active | Supersede D-004: use “T06 프로젝트 완주” as the initial real plan and minutes as the only duration unit. | It is genuine current work, safe to disclose when phrased without secrets, and directly supports Plan→Do→See. | T06-C07, T06-C17, T06-C25, T06-C32, T06-C78–T06-C81 |
| D-008 | Active | Use UUID primary keys, UTC database timestamps, and `Asia/Seoul` display and overdue rules. | IDs must survive refresh and Seoul-date comparisons must be deterministic. | T06-C04, T06-C14, T06-C23–T06-C24, T06-C30, T06-C35 |
| D-009 | Active | Enforce duplicate completion prevention with a client-supplied idempotency key and a database unique constraint. | Button disabling alone does not satisfy the official duplicate-click requirement. | T06-C21–T06-C22 |
| D-010 | Active | Use soft deletion for tasks so current counts exclude them while historical evidence remains traceable. | The See task count explicitly excludes deleted tasks and drill-down needs stable records. | T06-C13, T06-C28, T06-C83 |
| D-011 | Active | Require a client key of 8–100 characters, unique within each task. Serialize completion/reopen through a database task lock; commit the status transition and event together. Replaying an old key returns its original event without changing current status. A new key on an already completed task returns 409. | Handles concurrent duplicates and delayed retries after reopen without extra completion events. | T06-C11–T06-C12, T06-C21–T06-C22 |
| D-012 | Active | Execution logs are append-only with a created timestamp. Require offset-aware start/end instants and end strictly after start; store UTC, display Asia/Seoul. Actual integer minutes are entered independently of elapsed time. | Breaks can make actual working time differ from the time interval; neither task nor plan estimates are overwritten. | T06-C23–T06-C27 |
| D-013 | Active | Implement only the plan-wide current completed count and its source task IDs on the See endpoint in Card 3; extend it in Card 4. | C22 explicitly requires an observable See count. Historical completion events are not the current completed count. | T06-C22, T06-C29, T06-C83 |
| D-014 | Active | Extend D-013 to all seven metrics. Optional periodStart/periodEnd select tasks by inclusive due date within the selected plan; actual minutes include all logs of those tasks regardless of execution date. No period means all non-deleted tasks. Declare this rule in the UI. | Keeps the estimated and actual comparison on the same task cohort; the official source does not specify a period field. | T06-C28–T06-C32, T06-C83 |
| D-015 | Active | Compute all metric values and source records from one task/log outer-join result. Whitespace-only blockers do not count. Use Asia/Seoul today, current completion status, and distinct blocked task IDs. | Prevents join multiplication and mismatches between totals and evidence. | T06-C28–T06-C32, T06-C83 |
| D-016 | Active | Store an immutable reflection line and its date range. Copy its exact text into the new plan and atomically set next_plan_id while locking the reflection. Repeated creation returns the already linked plan. For whole-plan review, the recorded range spans plan dates and all included task due dates. | Preserves the user's judgment and prevents duplicate next plans on retries. | T06-C33 |
| D-017 | Active | Export every stored row in one JSON from a repeatable-read snapshot, including deleted tasks, normalized tag IDs and all history; emit explicit UTC offsets and map every column in the canonical contract. | Prevents incomplete or inconsistent exports. | T06-C35–T06-C36 |
| D-018 | Active | Serve built React and Flask API from one non-root Waitress Docker service with mandatory PostgreSQL, startup migrations and database health check. | Produces a tested deployment artifact; public hosting remains separate. | T06-C01, T06-C34–T06-C35, T06-C58 |
| D-019 | Active | Distinguish local from public evidence and leave actual-use entries and judgment lines for the user. | Synthetic tests and agent-written opinions cannot substantiate personal use or judgment. | T06-C59–T06-C60, T06-C78–T06-C81 |
| D-020 | Active | Use a new dedicated GCP VM for T06, following the user's CLOV deployment experience. Proposed size is e2-small with 2 GiB RAM; actual provisioning and cost estimate remain pending in the console. | The user explicitly chose another server after reviewing the existing CLOV VM's limited memory. | T06-C01, T06-C34–T06-C35 |
| D-021 | Active | Revise D-020's proposed size to e2-micro (1 GiB) for the user's minimum-cost preference; build images locally or in CI and measure runtime memory. Verify shared Free Tier allowance and additional IPv4/disk costs. | User requested a very cheap server or settings comparable to CLOV. Only CLOV's 1 GiB RAM is verified, not its machine type. | T06-C01, T06-C34–T06-C35 |
| D-022 | Active | Supersede D-020/D-021: use Render Free for the Docker web service and Neon Free PostgreSQL for about three months, subject to free quotas. Use a DB-independent /api/live for hosting probes and pre-ping reused connections. | User accepted the free combination; continuous DB probes would prevent idle scale-to-zero. No new GCP VM is needed. | T06-C01, T06-C34–T06-C36 |
| D-023 | Active | Adopt the reviewed CSS token design; bundle Gothic A1 through pinned Fontsource 5.3.0 with its OFL license, disable asset inlining, and strengthen evidence text contrast/size. | User approved the two review fixes and design adoption. Same-origin font files work with the existing strict CSP; readable sort/source labels support verification. Fontsource setup: https://fontsource.org/fonts/gothic-a1/install | T06-C05, T06-C06, T06-C15, T06-C20, T06-C58, T06-C82, T06-C83 |
| D-024 | Active | Put saved plans first, offer an optional new-plan form, and use one App-owned plan selection with sticky Plan/Do/See anchor navigation. Keep sections mounted between steps; reset task-local state on plan changes. Let execution forms use the full card width and constrain native input sizing. | User approved the recommended first three usability improvements. This preserves drafts during navigation, aligns task and summary scope, and fixes mobile datetime overflow while keeping the existing API and fixed expectations. | T06-C04–T06-C35, T06-C82, T06-C83 |
| D-025 | Active | Give the See metric cards a signed variance value and a per-kind evidence count. Keep the light default white-and-blue base: `varianceMinutes` is the only metric that takes a semantic colour (`--crit` over, `--good` under, neutral at zero); the other six stay neutral. Replace 근거 기록 보기 with the counts the drill-down actually lists, naming tasks and logs separately. | User chose to colour the variance card only, in both light and dark, after comparing a blue-mono and a three-card option. A single evidence total misread against the metric — 막힘 shows 2 tasks but is backed by 2 tasks and 3 logs. The value keeps the ASCII hyphen and the cards stay clickable. | T06-C28–T06-C32, T06-C83 |
| D-026 | Active | Add a two-state light/dark toggle in the header. The stored choice lives in `localStorage` under `t06-theme`; no stored value means follow the system. `main.tsx` resolves it before the first render and JS always writes a concrete `light`/`dark` onto `data-theme`, so the dark palette stays in one CSS block. The `prefers-color-scheme` media query is reduced to `--ground` and `--ink` under `:root:not([data-theme])` as a pre-mount fallback. | The user asked for a manual toggle. The deployment's strict CSP (`script-src 'self'`, D-023, T06-C58) forbids the usual inline pre-paint script, so the earliest available hook is the module entry; before it runs only the body background is visible, which the reduced media query paints. Writing a concrete value avoids duplicating the palette across a media query and an attribute selector. | T06-C82 unaffected; presentation only |
| D-027 | Active | Draw an estimate-vs-actual gauge on the selected plan card only, fed by one period-less `/api/plans/<id>/see` call that App owns. Name the two estimates apart on the card: `계획 예상` for the plan's own figure and `할 일 예상` for the task-estimate sum the gauge and See both use. | Actual minutes exist only on the See endpoint, so a gauge on every card would fan out one request per plan on each first paint of a free-tier deployment; the user chose the selected card. See's summary is period-filtered and must not drive a card that represents the whole plan, so App fetches its own unfiltered copy rather than sharing SeePanel's. The plan estimate and the task-estimate sum are different numbers (D-014) and appear together on the card, so each is labelled. | T06-C32 sign rule reused; presentation only |
| D-028 | Active | Mark the section being read in the existing sticky step bar with `aria-current="step"` and a filled accent background, instead of adding the T05 floating section rail. Look the section elements up on every measurement rather than capturing them once. | The rail earns its place on T05: a long single page with six sections and no other navigation. T06 has three sections and a sticky bar that also carries the plan picker, so a rail would duplicate navigation and compete with the public warning for the first screen (T06-C82). What was worth taking is the scroll spy: the three links looked identical and `aria-current` appeared nowhere in the codebase. Capturing the elements once breaks here because `TaskPanel` is keyed on the plan id and remounts on plan change, leaving a detached `#do-step` whose rect reads all zeros, which pins that step as permanently active. | T06-C82 unaffected; presentation only |
| D-029 | Active | Scroll to a step smoothly instead of jumping, via `scroll-behavior: smooth` on `:root`, and turn it off under `prefers-reduced-motion`. Give the step links a 150ms background/colour transition. | The document is over 4000px, so Plan to See was a teleport and the new current-step marker never appeared to travel; direction C was chosen because the screen states the ordered process, which the movement now shows. One CSS declaration covers both navigation paths, since `scrollIntoView` without an explicit `behavior` follows it. Animating thousands of pixels is exactly what the reduced-motion setting exists to stop, and the existing block only disabled transitions. | Presentation only |
| D-030 | Active | Let `T06_API_TARGET` override the Vite dev proxy target, defaulting to the previous `http://127.0.0.1:5000`. Read it with Vite's `loadEnv` and a `T06_` prefix, not `process.env`. | Port 5000 is not always free — another local app holds it on the author's machine — and the hard-coded target sent `/api` to whatever else was listening while the documented run steps still said to use 5000. `loadEnv` reads shell variables and `.env` files without pulling in `@types/node` for a single lookup. Dev server only: the deployment has no proxy because Flask serves the API and the built frontend from one origin, and the build output is unchanged. | None; development only |
| D-031 | Active | Call `db.engine` directly in `migrations/env.py` instead of Flask-Migrate's stock `db.get_engine()`-first fallback. | `get_engine()` still resolves on Flask-SQLAlchemy 3.x, so the template's fallback never ran and every migration emitted a DeprecationWarning — 12 across the suite. The method is due for removal in 3.2, and `deploy/start.sh` runs `flask db upgrade` under `set -eu` before serving, so its removal would stop the service from starting rather than fail quietly. pyproject pins Flask-SQLAlchemy>=3.1,<4, which makes the legacy branch unreachable by the project's own constraint. | Migration integrity per AGENTS.md rule 6 |
| D-032 | Active | Show only the selected plan as a full card and collapse the rest into one-line rows in a height-capped scrolling list, newest first. Reduce the task form to a title field with «자세히» revealing due date, priority, estimate and tags, and offer the plan's existing tags as clickable chips. | Every plan was rendered as a full card, so each pair of plans pushed Do and See about 330px further down: with 12 plans the list alone ran roughly 1,980px and Do began past 2,600px. Plan history that is rarely revisited was crowding out the two sections used daily. The task form was likewise always fully expanded, so every visit that only wanted to read the list had to scroll past it. The compact rows keep the `#plan-<id>` anchors that SeePanel's next-plan link jumps to, and keep the priority literal visible. | T06-C05, C15, C20, C82, C83 re-checked |
| D-033 | Active | Lay each task row out in three columns — status, body, actions — and move the execution-log toggle out of `ExecutionPanel` into that action group, mounting the panel only while open. | Rows were two columns, so the action buttons and the log toggle stacked under the content and each row ran 249px while about 70% of the row's width sat empty; six tasks filled 1,979px. Putting the actions in the empty right column costs nothing and hides nothing, which matters because 내용 수정 and 삭제 back T06-C10 and C13 and must stay reachable rather than move behind a hover. Only one log panel is open at a time now, which also keeps the list short. | T06-C09–C20, C23–C27 re-checked |
| D-034 | Active | Render the task list as one bordered box of 56px lines rather than a stack of cards, and reduce each row's three action buttons to always-visible 32px icon buttons — a clock for the execution log, a pencil for edit, a trash can for delete — with the Korean names kept in `title` and `aria-label`. Reflow to two lines under 760px. | With 18 tasks the card list ran 2,187px and repeated the words «Do · 실행 기록 열기» eighteen times down the right edge, so the action column read as a wall of text and the card borders competed with the list itself; rows are now 56px and the Do section fell from 2,656px to 1,496px. The actions are icons but not hover-only: hover does not exist on touch, and 내용 수정 and 삭제 are the evidence for T06-C10 and C13, so they must stay on screen — hover is used for emphasis, not for hiding. The one-line grid squeezes the title into three wrapped lines on a 375px screen, so narrow widths get an explicit two-line flex layout instead. | T06-C05, C09–C20, C23–C27, C82 re-checked |
| D-035 | Active | Style «자세히» through the shared secondary-button rule and name that rule as the single place a secondary button's appearance is defined. Keep it quieter than «추가» (`--ink-3`, no fill) and, under 640px, put the two buttons side by side instead of stacking all three controls. | The button matched no rule that set a border or background, so Chrome rendered it with its own 2px black outset chrome next to the blue primary. This is the second time a new button leaked out of `.actions button` — «내용 수정» and «삭제» did the same in D-033 — so the selector now carries a comment saying to add new buttons to it. Contrast is 4.63:1 light and 6.46:1 dark. Stacked full width, the optional button outweighed the primary one. | Presentation only |
| D-036 | Active | Stop clearing the plan summary before refetching it. Tag the stored summary with the plan id it belongs to and render the gauge only when that id matches the selected plan, and reserve the gauge's height so the card does not resize while the value is in flight. | Selecting another plan made the card flash: measured, its height went 310 → 231 → 264 → 231 → 264 on a single click. `setPlanSummary(null)` ran before every fetch, so the gauge was removed and the card collapsed; and because `dataRevision` is a dependency and `TaskPanel` — keyed on the plan id — remounts and immediately reloads its list on a plan change, the effect ran twice per click and blanked twice. Keeping the previous plan's numbers on screen was not an option, so the id tag decides what may be drawn and the reserved slot absorbs the wait. The card now holds 323px across every switch. | T06-C32 sign rule unaffected; presentation only |
| D-037 | Active | Drop `.task-actions button` from the text-button rule so `.icon-button` alone styles the row's icon buttons, and give the inline SVG `display: block`. | `.task-actions` now holds nothing but icon buttons, and the text rule's `padding: 8px 14px` outranked `.icon-button`'s `padding: 0` on specificity — (0,1,1) against (0,1,0). That left a 32px button with a 2px content box, so the 16px icon sat 15px from the left edge and 1px from the right; the same conflict gave the clock and pencil a visible border while the trash can, whose `.icon-button.danger` rule did win, had none. All three now measure 8px on every side. | Presentation only |
| D-038 | Active | Mark a task's due date only when it is overdue, today or tomorrow — `N일 지남` / `오늘` / `내일` — and keep the stored date beside it. Colour the marks red, amber, neutral. Judge the day against `Asia/Seoul`, like the See aggregates. | Eighteen rows of `2026-09-0x` told the reader nothing at a glance: the date had to be read and subtracted in the head to learn what was late. Marking every row would have been the same problem in new clothes, so the mark appears only when it changes what to do next, which is what makes it visible. The stored date stays on screen because T06-C14 asks for it there; the mark is added, never a substitute. Blue was rejected for the marks because in this design blue means action (D-025), so urgency runs `--crit` → `--warn` → neutral. | T06-C14 kept on screen; D-008 timezone rule reused |
| D-039 | Active | Move completed tasks out of the main list into a `완료 N개 보기` group below it, and open that group automatically when a task is completed or reopened. Do not split while a status filter is set. | A finished task held the top row of the list, above everything still to do. Collapsing it is only safe if the round trip stays reachable: T06-C11 and C12 need a completed task to be findable and reopenable, so completing one opens the group instead of making the task appear to vanish. Splitting under an explicit status filter would render a `완료` filter as an empty list, which is exactly what T06-C19 checks, so the filter wins over the split. | T06-C11, C12, C19 re-checked |
| D-040 | Active | Add `--crit-ink` and `--warn-ink` for text sitting on the matching `-soft` background, and use them for the priority badges and due marks. Fills, borders and the gauge keep the original `--crit`/`--warn`. | `--crit` on `--crit-soft` measured 3.82:1 and `--warn` on `--warn-soft` 3.86:1. At 11px the badges are not WCAG large text, so 4.5:1 applies and both fell short — a pre-existing shortfall in the priority badges that the due marks would have doubled. The worst pair is now 5.32:1 light and 5.95:1 dark. The dark palette already passed, so its ink values simply alias the originals. | T06-C05, C15 literals unchanged; presentation only |
| D-041 | Active | Draw the other-plans list as one bordered box whose items are separated lines, matching the task list, instead of a stack of individually bordered rows. | Four rows each carrying their own border and radius read as four cards floating in the section rather than one list. The task list was given the same treatment in D-034 and this brings the two lists into one shape. The 19rem cap and inner scrolling from D-032 are unchanged. | Presentation only |
| D-042 | Active | Filter the other-plans list by title, with the field appearing only once there are more than five other plans, and clear the query when a plan is picked. Show the match count alongside the total while filtering. | `GET /api/plans` has no limit and the screen renders every plan, so past six rows the list scrolls inside its 19rem box and the only way to find a plan is to scroll it. The field is withheld below that because with four plans it is noise. Only the title is matched, because the title is what the row shows — matching a hidden field would leave the reader unable to see why a row matched. Picking a plan is the end of the search, and a query left behind would keep the rest of the list hidden. | Presentation only |
| D-043 | Active | Give the priority badge a fixed 3.75rem width inside task and plan rows, and reflow plan rows to two lines under 640px. | The badges measure 35px (`low`), 40px (`high`) and 60px (`medium`), so the title column — the one the eye runs down — shifted by 25px from row to row in both lists. The plan card's badge stands alone and is left as is. On a 375px screen the plan grid gave the date range and the estimate a line each, making rows 94px; the two-line flex used for task rows brings them to 73px. | T06-C05, C15 literals unchanged; presentation only |
| D-044 | Active | Add a priority filter beside the plan name search, under the same «more than five other plans» gate, and combine the two with AND. Clear the search text when a plan is picked but keep the priority selection. | Priority is the only field on a plan that sorts it into groups a reader would ask for, and the task tools already offer the same control, so the two lists stay one vocabulary. The two controls are cleared differently on purpose: typing a name is «find this one» and is finished once the plan is picked, while a priority selection is «show me this kind» and dropping it would break the browse the reader was in the middle of. The heading reports matches against the total whenever either is active, and the empty message is now about conditions rather than names. | Presentation only |
| D-045 | Active | Add a sort control beside the plan search and priority filter, under the same «more than five other plans» gate: 최신순 (the existing newest-first order, kept as the default), 중요도순, 마감 임박순. Both new orders end with the Do list's tie-break chain — 우선순위 → 마감일 → 생성 시각 → ID. Sort a copy, never the array the default order is read from. Give the three controls a floor height. | The row already showed its date range but nothing on screen could act on it, and priority and deadline answer different questions: priority is the intent recorded when the plan was written, the deadline is the pressure now — a «높음» plan due next week and a «보통» plan due tomorrow are indistinguishable under the existing controls. 예상 시간순 and 이름순 were rejected: the first drives no decision a reader would make about a plan, and the second is what the search field already does. Reusing Do's tie-break keeps one sort vocabulary on the screen; T06-C20 fixes that rule for the task list only, so the plan list may take its shape without touching it. The default order is the reversed API array, so sorting it in place would have leaked the new order into the next render of 최신순. Chrome ignores `line-height` on `select`, and under 640px the sort control wraps onto a line of its own where flex `stretch` has nothing to match it against, so the height is a floor rather than a line-height. | Presentation only; T06-C20 task rule untouched |
| D-046 | Active | Supersede D-044: drop the plan list's priority filter and keep the name search beside the sort control. The empty-list message goes back to being about the name, and the heading reports matches against the total only while a search is active. | 중요도순 from D-045 now answers the question the filter was added for, and it answers it better: the filter hid every other plan to show one priority, while the sort keeps all nine on screen in priority order, so the reader still sees where the «보통» plans sit. Two controls doing overlapping work also had a cost — three controls wrapped onto two lines at 375px and the heading ran 100px tall; with two they fit one line at 61px. The user asked for the removal. | Presentation only; T06-C20 task rule untouched |
| D-047 | Active | Mark a completed task by filling its row with `--good-soft` instead of striking the title through. Add `--good-soft-2` to both palettes for that row's hover, and set the completed title to `--ink-2`. | The strikethrough made the one thing a reader still has to read hardest to read: reopening a task (T06-C12) and finding which execution logs belong to it both start from its content. Colour stays a state signal rather than decoration (D-025) — completion is a state — and the row is not distinguished by colour alone, since the filled check circle stays. Hover needed a second green because the completed row already occupies `--good-soft` and `--surface-2` would have washed the green out. `--ink-2` keeps the row quieter than an active one while staying readable on the tint. | T06-C11–C12 content stays legible; presentation only |
| D-048 | Active | Cap both task lists at the plan list's 19rem and scroll inside them, dropping the `overflow: hidden` that was only there to clip the corners. | The task list had no ceiling, so every task added 56px and pushed See down by that much for good; the plan list has been capped since D-032 and the two boxes sit on the same screen, so one growing and one not made them read as different kinds of object. The user chose parity for that rhythm over the nested-scroll objection, which was raised and answered. `overflow-y: auto` clips the radius on its own, so the old declaration is redundant. Known cost, left as the user chose: at 375px task rows are 110px against the plan list's 73px, so the same 304px box shows about 2.7 task rows against 4.2 plan rows. | T06-C09–C20 unaffected; presentation only |
| D-049 | Active | Replace every `input[type="date"]` with an in-house `DateField` — a text box that still accepts typing plus a calendar popover drawn from the token layer — and move the Seoul-today rule into a shared `lib/date.ts` that `TaskPanel` now imports. No date library is added. | The native picker's popup is browser chrome: no selector reaches it, so the one control on the screen that could not be designed was the calendar. Typing is kept because the native input allowed it and making someone who knows the date click six times is a loss. A library was declined to keep the submission's dependency list at react/react-dom plus the bundled font, and because what is needed here is one month grid and arrow keys. Correctness beyond the look: `pattern` only checks shape, so `2026-02-31` passed submission validation — `setCustomValidity` now carries the real reason, and all date maths goes through UTC midnight so no timezone can shift a day. | T06-C04–C07, C14, C28–C32 date values unchanged; presentation plus one validation fix |
| D-050 | Active | Extend D-049 to the two execution-log `datetime-local` inputs by giving `DateField` a `withTime` flag rather than writing a second component: the same popover gains an 시/분/초 row and a 「지금」 button, and the stored value keeps the `YYYY-MM-DDTHH:MM:SS` shape the native input produced. Picking a day leaves the time alone and keeps the popover open. | A second component would mean a second popover, and the two controls sit on the same screen — the reason the two lists were given one height in D-048 applies here too. Keeping the stored shape means `ExecutionPanel`'s `${value}+09:00` and the whole D-012 UTC path are untouched; only the control changed. The day-then-time order is the order they are entered, and resetting the clock to midnight on every day click would make correcting a date cost the time as well. The screen shows the value with a space and stores it with `T`, because the text box is read and edited by a person. | T06-C23–C27 values unchanged; presentation only |
| D-051 | Active | Stand the plan gauge's tick at a fixed 60% of the track instead of scaling the track to `max(예상, 실제)`. The fill is `실제 / 예상 × 60%`, clamped at 100% with a torn right edge when the run exceeds 1.67×. No tick is drawn when the estimate is zero. | The old scale gave the larger of the two 100% of the track, which broke the gauge in two ways. The baseline moved — the tick sat at 54% when over and at 100% when under, so no two plans could be compared and the reference line was never in the same place twice. And an overrun always filled the bar, so 10% over and 500% over looked identical; the magnitude survived only in the tick's position, whose relation to the overrun is 예상/실제, an inverse. A fixed baseline makes fill length mean «how many times the plan», linearly. Clamping is the price, so the clipped edge says so rather than reading as a bar that happens to be full; the exact figures stay in the heading and the sentence below. A zero estimate has no baseline to draw, and putting the tick at 60% anyway would have the screen assert a reference that does not exist. | T06-C32 sign rule unaffected; presentation only |
| D-052 | Active | Keep the three most recent reflections on screen and fold the older ones behind 「이전 회고 N건 더 보기」, reusing the task list's `.done-toggle`. Do not cap the section's height. | The list had no ceiling — 143px a card, so ten reflections push the export panel about 1,000px down. A height cap was the obvious fix and is the wrong one here: a reflection card expands into the 다음 계획 form, and putting that form inside a scroll box costs more than the length it saves. Folding keeps the section free to grow when a card opens while the resting height stays fixed. The most recent are kept because the API returns them oldest-first and the button that gets used — 이 회고로 다음 계획 만들기 — is almost always on the newest one. Same toggle component as the completed-task group, since both are the same gesture. | T06-C33 unaffected; presentation only |
| D-053 | Active | Extend D-051: draw the gauge as segments that meet at the baseline instead of one bar with a tick. 계획 구간 is `--ink-3`, the part past the baseline is `--crit`, and an under-run leaves the gap to the baseline as a `--good` hatch rather than a fill. A `--line-2` hairline sits under the segments so the baseline still shows when the track is empty. No hatch when there are no execution logs. | The tick was the highest-contrast element on the card — `--ink`, the heading colour, and 18px tall over an 8px track, so the reference line read as a defect. Making the seam between two colours carry the baseline removes the element instead of restyling it. It also fixes a second fault D-051 left alone: the whole bar was `--crit` when over, so the screen said all 165 minutes were wrong when only the 75 past the plan were — the overrun now has a length of its own. Colour stays a state signal (D-025): the planned stretch is not a state, so it is neutral. Zero logs is not a saving, and hatching it green would have the screen congratulate a plan that has not started. | T06-C32 sign rule unaffected; presentation only |
| D-054 | Active | Replace all seven `select` elements with an in-house `Select` — a button plus a listbox drawn from the token layer, capped at the same 19rem the other three lists use, with a search field on the plan picker. Keep the native type-ahead behaviour where there is no search field. | The open dropdown is browser chrome like the date picker's popup, so only the closed box followed the tokens and an OS list stood next to a calendar we had drawn ourselves. Six of the seven have three or four fixed options and did not need this on their own, but leaving them native would have put two kinds of dropdown on one screen — the inconsistency the whole day's work has been removing. The plan picker is the one that earns it outright: its options grow without limit and the native type-ahead only matches a first letter, so a search field inside the list is something native cannot do. Cost, stated rather than hidden: on mobile a native `select` opens the OS sheet with large targets and ours does not; option rows go to 12px padding under 720px to make up part of it. | T06-C19 filter behaviour re-checked; presentation only |
| D-055 | Active | End the frequently used `/app` page after Plan·Do·See and move profile, JSON export, password change and account deletion to a protected `/settings` route. Put a settings entry beside the signed-in account and keep logout available in the same top menu. | Export and account management are infrequent administrative actions, while their long forms permanently pushed the daily Plan·Do·See flow downward. A dedicated route gives the destructive deletion form enough context on mobile and a stable reloadable URL; a drawer or modal would compress that warning and introduce focus/escape behavior around a destructive action. `/settings` uses the same `RequireSession` gate and no-store SPA shell as `/app`, so this changes navigation rather than the security boundary. | T07-C97, C109, C114, C134 preserved; frontend 76 passed, backend 318 passed/4 skipped |
