# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
*.egg-info/
.coverage
htmlcov/

# Node
node_modules/
dist/
coverage/
*.tsbuildinfo
/frontend/vite.config.js
/frontend/vite.config.d.ts

# Secrets and local configuration
.env
.env.*
!.env.example

# Private diary data and exports
private-data/
private-exports/
*.private.json

# Editors and operating systems
.vscode/
.idea/
.DS_Store
Thumbs.db

# Temporary evidence and local databases
tmp/
/backend/instance/
*.sqlite
*.sqlite3
*.db

# Seeded design canvases are 2.5 MB build artifacts; keep only the sources
/design/**/plando-see-*.html

# Local submission drafts. The deck and the architecture brief still carry a
# pending five-day result, so `git add -A` must not sweep the binaries in ahead
# of the real 2026-09-11 numbers. Ignoring keeps the drafts on disk; the final
# files are settled through docs/process/T07-FINAL-SUBMISSION-CHECKLIST.md.
/output/
